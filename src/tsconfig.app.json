{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "node",
      "jquery",
      "bootstrap"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
